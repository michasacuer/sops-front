declare var clippyRef;
export { clippyRef };

/* export function clippyInit(){
  clippy.load('Clippy', function (agent) {

    var clippyRef = agent;

    console.log('pierogi');
    console.log(clippyRef);
    window.setInterval(function () {
      agent.animate();
    }, 8000);
    agent.show();
    agent.animate();
  });
}
 */